---
name: subagent
description: Delegate independent bounded work to isolated Pi child Sessions without changing the assistant persona.
---

Use the `subagent` tool only when a task is independent enough to benefit from
an isolated context. Choose `research` or `review` for read-only work and
`execute` for authorized implementation. Keep the parent Session responsible
for accepting the result and deciding the next step.
